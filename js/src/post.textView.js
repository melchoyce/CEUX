    // the blocks custom views ========================================================================================//

    // TEXT CONTENT BLOCK
    post.textView = post.BlockView.extend({
      tpl: '#wp-text',
      isEditable: true
    });
